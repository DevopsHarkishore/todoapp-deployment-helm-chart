apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Release.Name }}-todo-get-task-api-ingress
spec:
  ingressClassName: {{ .Values.gettask.ingressClassName }}
  rules:
    - host: {{ .Values.gettask.rules.host }}
      http:
        paths:
          - pathType: {{ .Values.gettask.paths.pathType }}
            path: {{ .Values.gettask.paths.path }}
            backend:
              service:
                name: {{ .Release.Name }}-todo-get-task-api
                port:
                  number: {{ .Values.gettask.port.number }}
                  
